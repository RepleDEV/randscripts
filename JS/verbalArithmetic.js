(function verbalArithmeticSolver(str) {
    str = str.toUpperCase().replace(/ /g, "");
    
    var str_equation = str.split("=");
    var str_operations = str_equation[0].split("+");
    var str_answer = str_equation[1];

    var answer_operations = [];
    var answer_answer = [];

    // turns out... i won't be doing this today gg
    
})("SEND + MORE = MONEY");